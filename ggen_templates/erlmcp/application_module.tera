{# =============================================================================
     Application Module Template (erlmcp_new_features_app.erl)
     ======================================================================== #}
-module({{ app_module | default(value="erlmcp_new_features_app") }}).
-behaviour(application).

%% Application callbacks
-export([start/2, stop/1]).

%%%===================================================================
%%% Application callbacks
%%%===================================================================

start(_StartType, _StartArgs) ->
    {{ app_module | default(value="erlmcp_new_features_app") }}_sup:start_link().

stop(_State) ->
    ok.

%%%===================================================================
%%% Internal functions
%%%===================================================================
