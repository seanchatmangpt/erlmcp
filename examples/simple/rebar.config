%% ============================================================================
%% Simple MCP Example - Makefile-based Build (v2.0)
%% ============================================================================
%% This example is compiled using the parent project's build system.
%% Use: make -C ../.. && cd examples/simple && make run
%% Or: From root: rebar3 shell, then: c(examples/simple/simple_server_stdio).
%% ============================================================================
%% For standalone builds, compile root project first, then use erlc directly.
%% ============================================================================

%% Minimal config for IDE/editor support
{erl_opts, [
    debug_info,
    {i, "../../include"}
]}.
