%% ============================================================================
%% Weather MCP Example - Makefile-based Build (v2.0)
%% ============================================================================
%% This example is compiled using the parent project's build system.
%% Use: cd ../.. && rebar3 compile && cd examples/weather && make run
%% Or: From root: rebar3 shell, then: c(examples/weather/weather_server_stdio).
%% ============================================================================
%% For standalone builds, compile root project first, then use make.
%% ============================================================================

%% Minimal config for IDE/editor support
{erl_opts, [
    debug_info,
    {i, "../../include"}
]}.
