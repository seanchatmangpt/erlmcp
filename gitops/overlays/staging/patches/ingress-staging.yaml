apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: erlmcp
  namespace: erlmcp-staging
spec:
  tls:
    - hosts:
        - staging.erlmcp.example.com
      secretName: erlmcp-staging-tls
  rules:
    - host: staging.erlmcp.example.com
