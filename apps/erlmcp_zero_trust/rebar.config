{erl_opts, [debug_info, warn_unused_vars, warn_shadow_vars, warn_unused_import, warn_obsolete_guard, warn_unused_function, warn_unused_record, warn_deprecated_function]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.

{deps, []}.

{profiles, [
    {test, [
        {erl_opts, [export_all, debug_info]},
        {cover_enabled, true},
        {deps, [
            {proper, "1.4.0"},
            {meck, "0.9.2"},
            {coveralls, "2.2.0"}
        ]}
    ]}
]}.